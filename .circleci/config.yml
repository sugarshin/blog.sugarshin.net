version: 2
jobs:
  build:
    working_directory: ~/blog.sugarshin.net
    docker:
      - image: circleci/node:10.13.0
    environment:
      TZ: /usr/share/zoneinfo/Asia/Tokyo
    steps:
      - checkout
      - run:
          name: System information
          command: |
            echo "Node.js $(node -v)"
            echo "npm $(npm -v)"
            echo "Yarn v$(yarn --version)"
      - restore_cache:
          name: Restoring Cache - dependencies
          keys:
            - v1-dependencies-{{ arch }}-{{ .Branch }}-{{ checksum "yarn.lock" }}
            - v1-dependencies-
      - run:
          name: Install dependencies
          command: yarn
      - save_cache:
          paths:
            - ~/workspace/node_modules
            - ~/.cache/yarn/
          key: v1-dependencies-{{ arch }}-{{ .Branch }}-{{ checksum "yarn.lock" }}
      - run:
          name: Lint
          command: npm run lint
      - run:
          name: Test
          command: |
            npm run test:coverage && node_modules/.bin/codecov
      - run:
          name: Build
          command: NODE_ENV=production npm run build
      - deploy:
          name: Deploy
          command: |
            if [ "${CIRCLE_BRANCH}" == "master" ]; then
              git config --global user.name 'CircleCI'
              git config --global user.email 's+circleci@sugarshin.net'
              npm run deploy
              curl -XPOST -d '{"text": "Blog has been updated! <https://blog.sugarshin.net>"}' $SLACK_WEBHOOK_URL
            else
              echo 'It was skip deployment because not master branch.'
            fi
