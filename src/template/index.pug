doctype html
html(lang= lang)
  head
    if googleSiteVerificationKey
      meta(name="google-site-verification" content= googleSiteVerificationKey)
    meta(charset="utf-8")
    title= title
    meta(name="viewport" content="width=device-width, initial-scale=1")
    if author
      meta(name="author" content= author)
    if description
      meta(name="description" content= description)
    if process.env.NODE_ENV === 'production'
      link(rel="alternate" type="application/atom+xml" title="Atom Feed" href="/feed.xml")
      link(rel="alternate" type="application/rss+xml" title="RSS 2.0 Feed" href="/rss.xml")
    if process.env.NODE_ENV === 'production'
      link(rel="stylesheet" href= process.env.MAIN_STYLESHEET)
    if favicons
      each tag in favicons
        != tag
  body
    if content
      div(style="display: none;")
        h1= title
        div!= content
    #app-root
    if process.env.NODE_ENV === 'production'
      script(src= process.env.MAIN_SCRIPT)
