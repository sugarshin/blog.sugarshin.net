@import '../../stylus/media-query'
@import '../../stylus/variables'

.wrapper
  padding 0 8px
  @media $mq-large
    padding 0 16px// @stylint ignore
    max-width $main-max-width
    margin 0 auto
.title
  margin 0
  padding 16px 0 8px
  border-bottom 1px solid #f3f3f3
  font-weight bold
  @media $mq-large
    padding 32px 0 16px// @stylint ignore
